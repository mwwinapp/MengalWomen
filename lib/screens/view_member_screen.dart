import 'package:flutter/material.dart';

class ViewMemberScreen extends StatelessWidget {
  final String mId;

  const ViewMemberScreen(this.mId);
  
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
